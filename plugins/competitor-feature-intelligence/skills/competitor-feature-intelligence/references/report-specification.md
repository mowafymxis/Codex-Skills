# Report Specification

Generate the Markdown executive report, app inventory JSON, source JSON, competitor features JSON, feature matrix CSV, main gap analysis JSON, roadmap, do-not-build, positioning, technical feasibility, metadata, optional change report, and optional issue drafts. Validate the main JSON against `schemas/feature-gap.schema.json`. Always include uncertainty, failures, source coverage, and partial-analysis warnings.
