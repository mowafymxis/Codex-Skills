interface:
  display_name: Database Change Safety
  short_description: Safe migrations, backfills, and rollbacks
  default_prompt: Use $database-change-safety to review this database change for mixed
    versions, locks, integrity, recovery, and rollout.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
