interface:
  display_name: API Contract Safety
  short_description: Protect API compatibility and consumers
  default_prompt: Use $api-contract-safety to review this interface change for compatibility,
    consumers, rollout, and contract tests.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
