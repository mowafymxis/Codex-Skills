# Agent Instructions

- Before editing unfamiliar areas, read `PROJECT_MEMORY.md`.
- If `PROJECT_MEMORY.md` is missing, run `context-compress scan`.
- After large architecture changes, run `context-compress update`.
- If memory conflicts with code, trust code and update memory.
- Treat assumptions as assumptions, not facts.
- Preserve manual notes between the manual note markers.
